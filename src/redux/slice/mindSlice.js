import { createSlice } from "@reduxjs/toolkit";

const mindState = {
  target:""
}

export const mindSlice = createSlice({
  name: 'mind',
  initialState: mindState,
  reducer:{
    
  },
  extraReducers: builder => {}
})